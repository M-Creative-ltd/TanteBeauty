# ============================================
# TANTE BEAUTY - Environment Variables Template
# ============================================
# 
# This file documents all environment variables used by this web.
# Copy this file to .env and update the values for your environment.
# 
# IMPORTANT: Never commit .env to version control!
# ============================================

# --------------------------------------------
# Keystatic Admin Authentication
# --------------------------------------------
# These credentials protect the /keystatic admin UI from unauthorized access.
# 
# REQUIRED: Yes (for production)
# SECURITY: Critical - Use strong, unique passwords
# 
# KEYSTATIC_ADMIN_USERNAME
#   Description: Username for accessing the Keystatic CMS admin interface
#   Example: admin
#   Production: Use a unique username (not "admin")
#   Location: Used in app/api/keystatic-login/route.ts
# 
# KEYSTATIC_ADMIN_PASSWORD
#   Description: Password for accessing the Keystatic CMS admin interface
#   Example: change-me-to-strong-password
#   Production: Use a strong password (min 16 characters, mix of letters, numbers, symbols)
#   Location: Used in app/api/keystatic-login/route.ts
# 
KEYSTATIC_ADMIN_USERNAME=admin
KEYSTATIC_ADMIN_PASSWORD=change-me-to-strong-password

# --------------------------------------------
# JWT Token Configuration
# --------------------------------------------
# These settings control JWT token generation and validation for admin authentication.
# 
# REQUIRED: Yes (JWT_SECRET is required for authentication to work)
# SECURITY: Critical - JWT_SECRET must be a strong, random string
# 
# JWT_SECRET
#   Description: Secret key used to sign and verify JWT tokens for admin authentication
#   Example: your-super-secret-jwt-key-min-32-characters-long
#   Production: Use a strong, randomly generated string (min 32 characters recommended)
#   Generation: Use a secure random generator (e.g., openssl rand -hex 32)
#   Location: Used in middleware.ts and app/api/keystatic-login/route.ts
#   Security: Never commit this value to version control or expose it publicly
# 
# JWT_EXPIRATION
#   Description: Token expiration time for admin session tokens
#   Default: '2h' (2 hours) if not set
#   Format: Number + unit (e.g., '2h', '24h', '7d', '3600s') or seconds as number
#   Examples: '2h', '24h', '7d', '3600' (3600 seconds = 1 hour)
#   Location: Used in app/api/keystatic-login/route.ts
#   Recommendation: '2h' for development, '24h' or shorter for production
# 
JWT_SECRET=your-super-secret-jwt-key-min-32-characters-long
JWT_EXPIRATION=2h

# --------------------------------------------
# Node Environment
# --------------------------------------------
# Automatically set by Next.js based on how you run the app.
# You typically don't need to set this manually.
# 
# NODE_ENV
#   Description: Defines the environment mode (development, production, or test)
#   Options: 'development' | 'production' | 'test'
#   Default: 'development' (npm run dev), 'production' (npm run build/start)
#   Location: Used in middleware.ts, app/api/keystatic-login/route.ts
#   Note: In production, secure cookies are automatically enabled
# 
# NODE_ENV=production

# --------------------------------------------
# CORS Configuration (Production Only)
# --------------------------------------------
# CORS headers are automatically applied in production mode only.
# Development mode has no CORS restrictions.
# 
# ALLOWED_ORIGIN
#   Description: The origin URL allowed to make CORS requests to your API routes
#   Default: https://tantebeauty.com (if not set)
#   Production: Set to your production domain (e.g., https://tantebeauty.com)
#   Location: Used in next.config.js for CORS headers
#   Note: Only applies when NODE_ENV=production
# 
# ALLOWED_ORIGIN=https://tantebeauty.com

# --------------------------------------------
# Next.js Public Variables (Optional)
# --------------------------------------------
# Variables prefixed with NEXT_PUBLIC_ are exposed to the browser/client-side code.
# Only use these for non-sensitive values that need to be accessible in the browser.
# 
# NEXT_PUBLIC_*
#   Description: Client-side accessible environment variables
#   Security: These values are visible in the browser - NEVER put secrets here!
#   Usage: Access via process.env.NEXT_PUBLIC_* in client components
#   Example Use Cases:
#     - API endpoints (if not sensitive)
#     - Analytics IDs
#     - Feature flags
#     - Public configuration values
# 
# Example:
# NEXT_PUBLIC_API_URL=https://api.tantebeauty.com
# NEXT_PUBLIC_ANALYTICS_ID=UA-XXXXXXXXX-X
# NEXT_PUBLIC_SITE_URL=https://tantebeauty.com

# --------------------------------------------
# Production Deployment Notes
# --------------------------------------------
# 
# When deploying to production:
# 1. Set KEYSTATIC_ADMIN_USERNAME to a unique username
# 2. Set KEYSTATIC_ADMIN_PASSWORD to a strong, randomly generated password
# 3. Set JWT_SECRET to a strong, randomly generated secret (min 32 characters)
#    Generate with: openssl rand -hex 32
# 4. Optionally set JWT_EXPIRATION (default is '2h')
# 5. Ensure NODE_ENV is set to 'production' (usually automatic)
# 6. Add any NEXT_PUBLIC_* variables if needed for client-side features
# 7. Store these values securely in your hosting platform's environment variable settings
# 
# Common hosting platforms:
# - Vercel: Project Settings → Environment Variables
# - Netlify: Site Settings → Build & Deploy → Environment
# - Railway: Variables tab in project dashboard
# - AWS/Docker: Use .env file or secrets management service
# 
# Security Best Practices:
# - Use different credentials for development and production
# - Rotate passwords periodically
# - Never commit .env files to git
# - Use a password manager to generate and store strong passwords
# - Enable 2FA on your hosting platform if available
